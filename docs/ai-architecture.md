# AI architecture

OrbitGuard does not currently include an AI, ML, retrieval-augmented generation, or external model service in its runtime path. Its “intelligence” endpoint is deterministic text and action selection derived from the risk and environment services. Any future AI capability must preserve source provenance, avoid presenting generated output as operational data, and remain subordinate to validated engineering models and human review.
