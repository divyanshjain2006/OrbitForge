# Retrieval architecture

No retrieval system is implemented. Scientific URLs in the documentation are references only; they are not fetched, embedded, or used to generate runtime decisions. A future retrieval feature would require source selection, versioning, licensing, citation display, evaluation, and a clear separation between retrieved context and authoritative mission data.
